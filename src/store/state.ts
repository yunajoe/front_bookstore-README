import { atom } from "jotai";

export const countAtom = atom(0);

export const pointVisibleAtom = atom(true);

export const addressCurrentPageAtom = atom(1);

export const bookCurrentPageAtom = atom(1);